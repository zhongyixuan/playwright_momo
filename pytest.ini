[pytest]
addopts = -v --browser chromium --html=reports/report.html --self-contained-html
pythonpath = .

markers =
    smoke: Quick sanity checks for core functionality
    search: Search feature tests
    filter: Filter and sorting tests
    edge: Edge cases and boundary tests
